# Force npm to rebuild native dependencies
unsafe-perm=true
rebuild-on-change=true

# Better handling of optional dependencies
optional=true

# Ensure native binaries are built for correct platform
target_platform=linux
target_arch=x64